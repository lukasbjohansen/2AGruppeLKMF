@page
@using RazorPageApplication.Models
@model RazorPageApplication.Pages.SchoolClasses.IndexModel
@{
}
<h1>Liste over klasser</h1>

<p>
    <a asp-page="CreateSchoolClass" class="btn btn-success btn-sm">Opret ny klasse</a>
</p>

@* <form method="get">
    search: <input type="text" asp-for="@Model.FilterCriteria" />
    <select asp-for="@Model.FilterBy" class="form-select d-inline-block" style="width:auto; margin-left: 8px;">
        <option value="All">All</option>
        <option value="FirstName">Firstname</option>
        <option value="SurName">Surname</option>
        <option value="PhoneNumber">Phonenumber</option>
        <option value="Mail">Mail</option>
        <option value="City">City</option>
    </select>
</form>
<br />
<!-- All -->
<input type="radio" asp-for="SelectedMemberType" value="" checked="@(Model.SelectedMemberType == null)" />
<label>All</label>
<br />
<!-- Enum values-->
@*Alle enums typerne løbes igennem og hver enkelt type tages ud og vises i sin egen radiobutton. De binder hver især til den public property SelectedMemberType. Det er value dvs. “@type” der assignes til den bagvedliggende og ligeledes det der vises.*@
@* @foreach (var type in Enum.GetValues<MemberType>())
{
    <input type="radio" asp-for="SelectedMemberType" value="@type" />
    <label>@type</label>
    <br />
}
<input type="submit" value="Filter" /> *@

<table class="table">
    <thead>
        <tr>
            <th>ID</th>
            <th>Navn</th>
            <th>Årgang</th>
            <th>Skole</th>
            <th>Lærer</th>
            <th>Slet | Redigér</th>
        </tr>
    </thead>
    <tbody>
        @foreach (SchoolClass item in Model.SchoolClasses) //Vi vil gerne løbe alle vores (public property) Member igennem og tage ét member ud af gangen (item) som vi smider ind i en række. Det gør vi ved at blande C# med html ved at bruge @
        {
            <tr>
                <td>@item.Id</td>
                <td>@item.Name</td>
                <td>@item.Year</td>
                <td>@item.School</td>
                <td>@item.Teacher</td>
                @* <td>
                    <a asp-page="DeleteSchool" asp-route-id="@item.Id">Slet skole</a> |
                    <a asp-page="EditSchool" asp-route-id="@item.Id">Redigér skole</a>
                </td> *@
            </tr>
        }
        

    </tbody>

</table>